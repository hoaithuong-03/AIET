services:
  - type: web
    name: ai-story-agent
    env: docker
    region: singapore
    plan: free
    healthCheckPath: /health
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: PORT
        value: 10000
